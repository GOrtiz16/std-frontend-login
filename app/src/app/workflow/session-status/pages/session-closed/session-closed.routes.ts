import { Routes } from '@angular/router';

import { SessionClosedComponent } from './session-closed.component';

export const ROUTES: Routes = [
  {
    path: '',
    component: SessionClosedComponent,
  },
];
